# Troubleshooting

## 整本整理出现 502 / 503 / 504 或超时

4.0.126 起，整本整理按 SillyTavern token 计数分批，每个 batch 只发送本批条目。Transport failure 会先机械拆小 batch，不会改成同尺寸 plain JSON 重发。

如果最终仍停在单条条目，通常说明该条目本身过大、provider 太慢或网关限制过紧。先检查该条目长度、模型上下文/输出上限和中转超时。

## 整本整理提示 JSON / E 引用错误

镜渊允许代码围栏、前后说明、尾逗号等外层格式漂移，但不会放松内容完整性。以下情况会零提交：

- `complete` 不是 `true`；
- E 数量不一致；
- 未知、重复或缺失 E；
- 某个非空来源没有返回任何事实。

这类失败不能通过猜测引用或把遗漏理解为删除来恢复。

## 小总结长期没有触发

检查提取模型是否正常产生稳定的 `场景` 条目：首次场景必须建立；同一连续场景保持同一名称；真正切场时需要输出新的场景条目。ST 召回旧场景条目不会触发切场。

## 世界书保存后看起来没有变化

镜渊保存后会直接 fresh-read ST 后端并比较提交指纹。若显示保存核验失败，应先检查 ST/存储层，而不是假定 UI 缓存已经成功写入。

## Edit / Swipe / Delete 后的恢复范围

镜渊使用当前 SillyTavern `world_info_depth` 作为有限历史补建边界，不会无限重放到聊天尾部。
