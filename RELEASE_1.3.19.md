# Mirror Abyss 1.3.19

流水线：`ma-pipeline-82`

本版修复世界书控制提示泄漏。旧发布内容会把表名、字段标签、摘要标签、生命周期、关键词和 `[MA11] MA｜...` 备注一同交给主模型，模型可能在角色正文中模仿“标题约束、摘要、镜渊”等词。

新版世界书只发布自然事实值；备注不再进入提示，托管身份全部留在不可见扩展元数据。升级后执行一次立即同步即可改写现有托管条目，无需重新提取表格。
