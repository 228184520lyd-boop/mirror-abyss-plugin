# Validation

版本：`1.1.0-alpha.4`

已完成：

- TypeScript `strict` 静态检查通过。
- ESM 生产构建通过。
- `node --check index.js` 通过。
- 18 项自动测试全部通过。
- 空快照包含九类表格。
- 人物生命周期字段归一化通过。
- 模型漏写人物时保留正式人物通过。
- 同名人物复用旧稳定 ID、避免重复建档通过。
- 玩家手动行新增、锁定、编辑和删除通过。
- 小总结沉降协议解析通过。
- 仅删除已结束自动行、保护活动行和玩家锁定行通过。
- 人物活跃状态只允许逐级休眠/归档通过。
- 对象语义世界书命名和内容映射通过。
- 物品与资源、技能与能力按所有者命名通过。
- 流程和事件分类通过。
- 已被大总结吸收的小总结退出当前周期条目通过。
- 累计大总结数据链字段通过。
- 关系图谱生命周期数据通过。
- APP_READY 初始化、设置面板单例和事件监听单例通过。
- 同一正文重复触发时状态模型只调用一次。
- 移动端表格保持横向排版与滚动。
- 自定义主题文字/面板变量隔离通过。
- 静态确认未监听 `GENERATION_ENDED` 执行业务任务。
- 静态确认插件设置中没有 API Key 字段。

尚未完成：

- 真实 SillyTavern 云部署中的对象语义世界书写入与旧条目清理测试。
- 不同 Connection Profile 的真实调用测试。
- 真实模型对生命周期 JSON 协议的长期遵循测试。
- 100 回合以上沉降与累计大总结测试。
- 群聊、复杂删除序列、跨设备并发测试。
- 手机浏览器长时间后台运行测试。

因此本版本仍标记为 alpha，不宣称长期稳定版。
