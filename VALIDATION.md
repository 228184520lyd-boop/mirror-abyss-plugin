# Validation — 1.1.0-alpha.10.5.5 Plain-Text Native Protocol

## 自动与工程验证

- 自动测试：103/103 通过。
- TypeScript strict：通过。
- 依赖边界检查：通过。
- ESLint：通过。
- 生产构建与 `node --check index.js`：通过。
- npm 已知漏洞：0。
- package、manifest 与运行时版本：`1.1.0-alpha.10.5.5`。
- 测试聚合器兼容 ANSI 彩色输出，当前目录与洁净源码均正确汇总 103/103。

## 纯文本协议验证

- 统一事实、状态、小总结和大总结标签协议解析通过。
- 端到端正文审核后事实提取使用纯文本协议。
- SillyTavern `generateRaw` 请求不包含 `jsonSchema`。
- JSON 格式修复运行时已删除；旧设置自动丢弃。
- 旧 JSON 返回只读兼容通过。
- HTML 错误页与 `Unexpected token '<'` 错误识别为真实上游错误。
- 连接测试使用 `MA_CONNECTION_OK`，不要求 JSON。

## 玩家流程

- 30 轮：审核 30、事实 30、小总结 6、大总结 3，总调用 69。
- 100 轮：审核 100、事实 100、小总结 20、大总结 10，总调用 230。
- 120 条历史重建：10 次批次事实/阶段总结＋1 次最终大总结。
- 缓存完整的手动重建仍可 0 次模型调用。

## 提示词体积合成样本

- 108 行表格事实提示词：57,577 → 37,741 bytes，减少 34.5%。
- 小总结：57,134 → 27,048 bytes。
- 大总结：42,276 → 11,493 bytes。

这些是合成样本，不是所有实际聊天的固定比例。

## 实机边界

仍需真实 SillyTavern 验证不同模型的标签遵循、真实 429/504、移动端后台回收和不同版本世界书编辑器刷新。

执行日期：2026-07-13
