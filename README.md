# Mirror Abyss 4.0.82

短代码版。世界书仍是唯一长期事实源；核心职责收敛为“入口 → 调 → 读 → 处理 → 写 → 回读 → 结束”。

4.0.82 修复功能出口：
- 模型响应统一解析，按钮与自动提取真正进入同一核心处理函数；
- MESSAGE_RECEIVED 自动提取改为串行等待，不再因 busy 静默丢任务；
- 世界书保存后刷新宿主列表/编辑器并回读；
- 提取按栏目机械落点，不再覆盖未返回栏目；
- 小总结/大总结/合并使用 E1/E2 临时编号确定性写回原 UID；
- 基石被核心实际读取并阻止自动/人工修改，解除基石是唯一解锁入口；
- 焦点投影为 ST 原生常驻召回，次关键词同步 selective；
- 主聊正文下方恢复镜渊状态条；
- UI 字号整体增大，保留白/灰高透明玻璃与轻量动态。
