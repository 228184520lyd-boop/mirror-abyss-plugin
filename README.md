# Mirror Abyss / 镜渊

版本：`4.0.34`

Mirror Abyss 是 SillyTavern 的长期游玩记忆扩展。世界书是唯一长期事实源；模型负责语义整理，插件只负责固定协议、精确身份、UID、单一事务入口、任务编排和界面。

## 单一链路

```text
UI或宿主事件 → RuntimeLease / ContextLease → Controller → OperationCoordinator → EffectEnvelope → 业务服务 → HostAdapter/WorldbookRepository → AuthorityOutcome三态 → AuthoritativeCommitModel → RuntimeReadModel → UI
```

- 提示词、协议校验、正文栏目和界面类型均读取 `src/core/schema.js`。
- 世界书读取、写入与回滚只经过 `WorldbookRepository`；只有带镜渊标记的世界书条目才归镜渊管理。
- 每次提交前后都从后端权威回读；SillyTavern 内存缓存不能单独构成写入成功。
- 聊天运行元数据只经过 `HostAdapter` 的按聊天串行保存口，并按绑定世界书 `scopeId + owner + revision` 分区；改绑世界书不会复用旧场景、组、历史证据或回执。扩展设置只经过 `SettingsStore`，其宿主 debounce 结果是内存投影，不冒充可权威回读的持久化回执。
- 条目身份严格使用“类型＋稳定名称”；不做相似度、包含关系或别名猜测。
- 模型不读取或输出 UID。
- 世界设定粘贴文本与 TXT 共用“AI整理 → 预览 → 确认写入”链。
- 世界设定、提取和总结均使用自然事实行协议，不使用额外对象外壳。
- 每个条目展开后的关键词/召回设置提供“定义前”“定义后”和“设为/解除基石”；基石只阻止 AI 自动覆盖与普通删除，玩家仍可直接编辑正文和设置。

## 4.0.34 独立复核根因闭合

- 自动建书不再直接调用第二个世界书保存原语。`WorldbookRepository` 现在拥有唯一 `saveWorldInfo()` 原语；创建空世界书、普通事务提交和回滚都从这一适配器边界进入。
- 自动建书的“创建 → 权威书目/正文确认 → 绑定”成为同一可恢复生命周期。创建一旦可能落盘就携带 `kind=provision` 恢复回执；绑定未提交时只删除仍保持创建 revision 的未绑定空书，玩家已经修改的候选书绝不自动删除；绑定结果无法唯一判定时进入现有 `RecoveryRegistry`，不再把孤儿书当作允许状态。
- `RecoveryRegistry` 在同一聊天内跨 worldbook scope 查找持久恢复证据，并同时包含 active/archive 回执；这只扩大恢复证据登记处的可见范围，普通运行态与 UI 仍只读取当前 scope 的 committed snapshot。
- 提取在模型调用前冻结完整权威世界书 revision，提交时将 `opened.digest` 传给现有 `WorldbookRepository.transact(expectedDigest)`。模型等待期间世界书被玩家或宿主修改时，旧模型结果以 `STALE_WRITE` 停止，不能再重新读取 live 状态后覆盖新事实。
- 新增防回退测试：生产源码只允许一个 raw worldbook writer；自动建书失败必须有补偿或 provision recovery 出口；跨 scope/归档恢复证据仍可发现并清除；提取模型等待期间的玩家世界书修改必须被 CAS 保留。

## 4.0.33 统一模型覆盖与职责闭合

- `WorldbookScope` 只保存世界书作用域身份与 revision；长期剧情正文只存在于当前聊天绑定世界书。场景组只保存 `groupId + revision + UID 集合` 等机械坐标，不保存条目正文副本。
- 提取模型固定接收提取前完整权威世界书、本轮玩家输入和当前 AI 正文；系统不再先按关键词或数量挑选“相关条目”。
- 小总结、大总结、玩家选中总结与人工合并共用同一完整批次整理协议：系统冻结当前权威 UID → 一次请求发送全部完整条目 → 协议校验 → 单一世界书事务提交。小总结不再拥有逐来源第二协议，也不存在可成功返回的空总结。 小总结提交并完成运行状态 finalize 后形成已结算组；同一自动父操作只在小总结耐久闭合后刷新状态并串行评估大总结。大总结只从已结算组取得当前仍存在的 UID，再从当前权威世界书读取完整正文。
- 总结格式失败只允许一次干净协议重试；第二次请求携带第一次的具体协议错误。模型请求、协议失败与事务提交互不重跑。
- `SemanticWorker` 只持有冻结输入、Prompt、模型调用与协议结果，不读取 Host 运行状态，不拥有 UID stale/collision/revision/recovery/UI/事务判断。
- 所有持久副作用仍由 `OperationCoordinator → EffectEnvelope` 单 mutation lane 执行；宿主世界书、metadata、chat、settings 原语分别只存在于 adapter 所有者。
- `RecoveryRegistry` 是唯一恢复证据源；Import、Migration、Reset、Diagnostics 的不确定提交或补偿失败均汇入同一 Registry。
- `AuthoritativeCommitModel` 只在顶层效果闭合后发布 committed snapshot；`RuntimeReadModel` 是 UI 唯一运行态读源，世界书页的运行徽标也从同一 committed snapshot 投影。
- `RevisionedReconcile` 是 UID、场景组和文件夹派生投影唯一同步路径。外部世界书更新与排队期间的文件夹布局变化都在同一 mutation lane 内按最新 revision 重算。
- 玩家明确人工合并或删除时在同一权威事务内解除所选条目的基石/焦点保护后执行；自动流程仍尊重保护。
- `WORLDINFO_UPDATED` 的 reconcile 失败不再被事件处理器吞掉：直接调用得到真实失败，宿主后台事件由统一后台错误出口接管并发布失败。
- 新增统一模型防回退测试，禁止剧情正文副本、提取 Top-N 语义筛选、小总结第二协议、UI live-state 旁读、第二 EffectEnvelope 入口及 adapter 外持久化原语重新进入生产源码。

## 4.0.32-recovered.8 基线（已由 4.0.33 收口替代）

4.0.32-recovered.8 建立了 `DefenseLayer`、`SemanticWorker`、`RecoveryRegistry`、`AuthoritativeCommitModel`、`RuntimeReadModel`、`EffectEnvelope` 和 revisioned reconcile 的统一模型骨架；4.0.33 删除仍在这些模型旁边运行的旧职责与第二状态源，并以当前冻结架构为唯一维护合同。

## 4.0.31 功能

- 修复 UI 任务运行中重绘的内部竞态：新建的同任务按钮仍会投影共享 busy，但 capability 层不再把这次临时禁用误记为固有禁用；任务结束后恢复按钮与普通任务按钮都会回到真实能力状态，不会出现队列已空闲、恢复出口却永久灰掉。

## 4.0.30 功能

- 自动小／大总结失败卡重试成功后，会在同一运行状态事务中完成产物发布、失败卡删除、阶段闭合和总状态终结；旧的模型错误不会继续压住新的成功终态，尚未执行的大总结也不会被误报为刚完成。
- 手记页无恢复围栏时不再把空恢复提示作为 DOM 参数插入，浏览器不会显示字面量 `null`；测试桩同步采用真实 `Node.append` 的字符串化语义，避免再次漏检。

## 4.0.29 功能

- 连续 Edit、Swipe、Delete 不再彼此取消较早的回滚范围：同一聊天身份内合并为一个来源变化批次，保留最小消息边界，并逐条保留稳定等待和重处理意图。快速到达的多条 `MESSAGE_RECEIVED` 在同一 debounce 窗口去重后全部串行执行，不再只留下最后一条。
- 聊天身份统一为完整 `stateOrigin`（聊天键、角色／群组身份、绑定世界书）。模型等待、补偿、原聊天状态写回和权威核对在每个异步出口重新验证同一身份；同名聊天不能再跨角色误写。
- 世界书、正文和聊天元数据共用 `committed / not-committed / mixed` 权威三态。只有精确未提交允许普通失败／受控重试；可读第三态和保存后未知都携带回执或 `authorityProbe` 进入恢复围栏。回执核对同时原位修复 SillyTavern 隐藏世界书缓存，避免“后端未提交、提示词仍使用幽灵事实”。
- 消息写锁提升到整份聊天文件，符合 `saveChat()` 的真实资源粒度；权威消息恢复允许目标之后的纯尾部追加，但前缀修改、重排或删除仍停在 `mixed`，不猜测覆盖。
- 自动建书在创建、书目回读、编辑器刷新和绑定前后持续复验来源；玩家在等待窗口内手动绑定时人工选择优先。带旧 `expectedName` 或旧回执的任务会在 provision 前拒绝，不再先给替换聊天创建世界书再报错。
- UI 新增 `UIScope / ContextLease`。切聊、换书、重置或卸载会统一取消旧弹窗、文件读取、按钮 busy／不确定投影和异步续体；TXT 还同时校验导入 revision。文件夹重命名与移动在确认后重读最新布局，只重放本次意图。
- 已提交回滚／撤回若在运行元数据 finalize 阶段失败，会生成有限的 `rollback-finalize` 恢复证据，而不是留下“世界书已撤、回执仍在”的半状态。自动总结失败作用域包含完整聊天身份、世界书和 `groupId@revision`，旧聊天迟到结果不能阻塞新聊天同 UID 批次。
- `OperationCoordinator` 的 queued／terminal 事件对同步重入保持原子：监听器关闭队列、重复入队或取消终态都不会产生孤儿任务、同键双执行或改写已完成终态。

## 4.0.28 功能

- “角色定义位置”标题独占一行，“定义前 / 定义后”各占一半可用宽度；按钮保持原字号，边框提高到 48px 最小高度并可由内容撑开，不再出现框小于文字。
- 所有有副作用的任务以 `TaskQueue.enqueue` 为开头，以事实回读、运行状态或恢复回执全部落定为结尾；人工任务优先于自动总结，自动任务不能让人工操作饥饿。外部世界书同步、迁移扫描和失败总结重试不再在队列外留下迟到步骤。
- 同一 JavaScript realm 只允许一个 `RuntimeLease`；跨适配器实例的世界书创建、书级写入与聊天元数据保存共享锁。取消若跨过外部提交点，必须完成权威对账或补偿，不能直接报成未写入的取消。
- 正文、聊天运行元数据或世界书绑定保存后无法确认时，会用提交前／预期快照建立当前 realm 的统一写入围栏；运行、手记和维护页禁用同一组 `WRITE` 能力，只有运行页保留一个“核对并恢复”出口。“仅审核”也属于 `WRITE`，因为审核不通过时会修正并保存 AI 正文，不能在消息提交待核对时旁路执行。权威结果为已提交或未提交时修复本地投影并解围，混合态或核对读取失败继续隔离。
- Edit 与已存在候选的 Swipe 使用 `target` 消息稳定模式，允许目标之后继续追加消息；Delete 使用 `changed` 模式确认删除边界；严格 `exact` 模式保留给必须冻结完整序列的调用。三种模式都要求连续两次一致的权威结果。尚未生成正文的 overswipe 只等待旧候选回滚，新正文仍由唯一的 `MESSAGE_RECEIVED` 链处理。
- 操作实现阶段统一归入四类语义终态：成功、失败、取消／已补偿、不确定／待恢复。退出前所有仍在运行的审核、修正、提取、写入、小总结和大总结子阶段都会闭合；重载时遗留的运行态归一为取消，不在 UI 留下没有出口的“处理中”。
- 删除模型调用、导入分块和世界书提交相邻层中重复的取消／上下文检查；只在输入快照、外部效果提交点和完成出口保留校验。纯读取、生命周期取消、派生界面刷新及提交补偿是明确且不写第二份事实的必要旁路。

## 4.0.26 功能

- “撤回”统一选择当前世界书最新一笔可逆提交，自动提取、自动总结、手动总结、导入与玩家编辑按实际落盘顺序逐次逆转；后写的自动小总结会先撤回，不再拿旧回执撞上已变化条目。
- 基石改为单纯的自动写保护。玩家在上锁期间可修改正文、召回、主焦点、角色定义位置并手动校准 ST 插入顺序；这些操作直接进入一次权威事务，不再先做一遍重复世界书读取。玩家明确删除在同一事务内解除所选保护后执行，危险确认后的完整重置可一次清理全部管理条目。
- 最新 AI 正文下方的两行轻量状态拥有独立自适应高度边框，不再继承宿主按钮固定高度而让第二行文字溢出。
- 对审核、提取、总结、导入、条目管理、基石、插入、迁移、诊断、撤回和重置做了端到端路径核对；实际写入仍只经过现有 Controller、业务服务与 `WorldbookRepository`，没有增加第二套事实、保存或撤回链。

## 4.0.25 功能

- 修复自动小总结不会稳定触发：每次提取都由模型返回独立的“当前场景”锚点，明确换场时即关闭上一场景组并运行小总结，不再依赖模型恰好生成一条“场景”世界书事实。
- 场景锚点只保存在现有聊天运行元数据中，不写成长期世界事实；即使换场当轮没有新事实，也会留下无需改写世界书、但可随 Edit、Swipe 或 Delete 精确撤销的运行回执。
- 最新 AI 正文下方的小号提醒会显示自动小总结“刚完成”或“失败待重试”，让实际执行结果可见；场景锚点也不会再因世界书刷新时没有同名场景条目而被清空。

## 4.0.24 功能

- 最新 AI 正文下方的镜渊轻量状态条增加小号总结提醒：小总结明确显示在场景切换后触发，大总结显示当前已完成组数与第 N+1 组触发进度；有待处理场景或自动功能关闭时同步切换提示。
- 自动运行设置明确说明小总结、大总结和大总结批次组数的真实触发关系，不改变既有场景分组、总结顺序或世界书写入链。

## 4.0.23 功能

- AI 提取与世界设定导入会为每个候选条目给出 4—8 个高辨识度主要关键词，包括名称、别称、缩写、常见指代和核心概念；关键词仍写入 SillyTavern 原生 `key`，不增加第二套召回。
- 新关键词只与原生名称、完整标题和玩家已有关键词合并，绝不覆盖手动填写；小总结、大总结和手动合并继承来源关键词，保留原有触发路径。
- 世界设定导入预览直接显示每个候选条目的主要关键词。角色定义前后、常驻策略、插入排序、扫描深度与世界书 Token 预算均保持不变，继续由现有插件规则或 SillyTavern 原生链负责。

## 4.0.22 功能

- “定义前”“定义后”和“设为/解除基石”统一移入每个条目的关键词/召回设置区，直接操作当前条目；批量管理栏不再重复显示这些按钮。
- 插入位置改为两个直接按钮，不再打开选择弹框；基石条目保留可用的“解除基石”。4.0.26 起基石作为自动写保护，不再禁用玩家召回与插入设置。
- 批量管理栏缩减为“移动文件夹”和“更多操作”，进一步减少手机首屏占用。

## 4.0.21 功能

- 只有经“世界设定导入”新建的条目由系统默认写在角色定义前；后续自动提取、总结和常规新增统一默认写在角色定义后。玩家可在手记管理态批量切换“定义前 / 后”，选择会随正文更新和插入校准持续保留。
- 六类条目在角色定义两侧共用唯一的粗到细顺序区间，因此玩家把任一类型移到定义前或定义后后，仍保持“基础设定→世界→场景→人物→物品→事件”。
- 手机端管理区改为四个高频入口加折叠的低频操作；类型筛选保持单行横向滑动，并增加全选、清空和独立的新建文件夹入口，减少首屏按钮堆叠。

## 4.0.20 功能

- 所有询问框改用浏览器原生 `dialog`，由浏览器负责居中、遮罩和手机视口适配，修复弹框跑到顶部及确认按钮无法操作。

## 4.0.19 功能

- 手记批量管理增加“移动到文件夹”，可将已选条目一次移入指定文件夹或默认分类；只更新 UID 文件夹布局，不改世界书事实。
- 新建与移动文件夹弹框增加内边距、字段间距和按钮间距，手机端按钮平分可用宽度。

## 4.0.18 功能

- 关联对象写入同一原生世界书正文的规范“镜渊关系”块；SillyTavern 开启递归且条目原生递归开关允许时，由宿主自己的递归扫描继续命中关联条目，不增加第二提示词或第二召回链。`无`、`none`、`null` 等空关联在协议边界统一归一为空数组。
- 大总结仅将非当前、非常驻的远端历史场景设为向量；把历史场景切换为原生常驻时同步关闭向量，禁止“常驻＋向量”。
- 手动小总结、手动大总结和手动合并把输出 UID 原子映射回原场景组，使其继续参与后续大总结；消息回滚只清理实际事务涉及的 UID，保留未受影响旧组。
- 大总结按条目更新时间和 UID 稳定排列来源；整理输出继承来源中最新的消息锚点，手动点击不会把远期条目误排成近期事实。
- 部署 ZIP 内固定为唯一 `mirror-abyss/` 根目录；所有归档目录为 `0755`、普通文件为 `0644`，打包后会校验顶层结构和 Unix 权限元数据。

## 4.0.17 功能

- 保持现有排版、分页、按钮与单滚动结构不变，仅将面板材质改为白色半透明玻璃，并为外框、页签分隔、启动徽记与运行状态加入非对称“镜渊遗迹终端”视觉层；装饰层不接收点击，也不参与布局尺寸。
- 增加低成本边框光流、徽记呼吸与运行状态脉冲；系统启用“减少动态效果”时会全部停用动画并保留静态边框。
- 异步审核、提取、总结、导入及世界书刷新在提交运行状态前校验原聊天与原世界书；取消或切换聊天后的迟到结果直接丢弃，不会写进新聊天或残留可重试失败。
- 运行页没有整理失败记录时不再把空值渲染成字面量 `null`。

## 4.0.16 功能

- 提取只接受正文叙事层中本轮新发生且已经成立的增量事实；旧事实核对、世界书诊断、冲突说明、建议、修正方案和待玩家确认内容不再写入。
- 已有对象必须复用相关条目的原生类型与稳定名称，避免为了覆盖类型而把同一对象重复建成错类型条目。

## 4.0.15 功能

- 同一类型的条目使用最后一次事实活动轮次生成原生 `order`：预算评估优先保留近期事实，ST 最终组装仍按远期→近期排列；六类继续固定为“基础设定→世界 / 场景→人物→物品→事件”，数值始终位于 ST 原生 0–9999 范围。

- 修复生成新 Swipe 时先回滚后又提取旧候选的问题：尚未完成的 overswipe 只回滚旧事务，新正文仍只由 `MESSAGE_RECEIVED` 单链处理；切换已有候选继续即时重处理。

- 修正提取、导入和总结共用的栏目规则提示：栏目名现在明确为当前 Schema 的封闭枚举，必须逐字复制；移除会诱导模型输出不存在“外观”栏目的错误示例，不放宽严格解析器。

- 次关键词保存要求完整的原生载荷，移除对旧原始字段的兼容回退；无效 `selectiveLogic` 会指出 UID 和原生字段，不再静默显示成另一种语义。

- 主、次关键词输入保留 SillyTavern 正则表达式内的逗号（如 `/a{1,3}/u`），并显式提示旧条目 `selective=false` 时次关键词尚未生效。

- 玩家审核提示词改为输入时立即进入原有设置保存链，避免切换运行/手记/维护页面时浏览器尚未提交 `change` 而丢失本次规则。

- 提取提示词明确区分当前AI正文、玩家意图与旧世界书上下文，并要求对已发生且有结果/影响的一次性行动保留事件事实，减少旧状态被误写为本轮变化或事件被其他类型吞并。

- 召回设置完整映射 SillyTavern 原生主要关键词 `key`、次关键词 `keysecondary` 与四种 `selectiveLogic`；保存时维持宿主要求的 `selective=true`，编辑、世界书写入、权威回读和基石保护均复用同一现有链路。

- TXT 导入改用显式可见的 44px 文件选择按钮，避免被 SillyTavern 的全局文件输入样式隐藏；仍与纯文本共用唯一 AI 预览与世界书写入链。

- 手机端筛选、批量管理、文件夹、条目工具与召回设置统一改为换行布局，消除第二条横向滚动，并将筛选触控高度固定为 44px。

- 用插件内唯一 DOM 模态层替换浏览器原生 `prompt/confirm`，确保文件夹、校准、合并、删除和重置操作可在 SillyTavern 内嵌浏览器中实际执行。

- 明确区分模型协议中的“类型”与“栏目”，避免把模板说明“人物：身份”误写进类型字段。
- 入口使用与清单版本一致的 `app.js` 缓存键，构建产物同时写入同一版本标记。
- 构建、校验和打包忽略 macOS AppleDouble 元数据，外置卷上不再把 `._*.js` 当作源码。

- 可选择 SillyTavern 连接配置并执行真实 API 测试；不会切换宿主全局连接。
- 单执行队列统一自动处理、人工处理、总结、回滚与写入；可以取消当前及全部等待任务。
- 聊天消息显示审核、修正、提取和写入状态；自动总结失败保留精确来源并可逐项重试。
- AI 正文 Edit/Swipe 先按 UID 回滚再处理新正文；用户消息 Edit 与 Delete 只回滚受影响写入。
- 世界书原生常驻、主要关键词与两项递归开关可查看和编辑；内容更新不会覆盖玩家设置。
- 向量召回只用于大总结后的远端历史场景；人物、物品、事件、世界与基础设定不会因大总结被误转为向量。
- 原生插入只写 `position/order`：世界设定导入的新条目默认位于角色定义前，之后产生的新条目默认位于角色定义后；玩家可在条目关键词/召回设置中逐条切换，任一侧均按六类粗到细排列。SillyTavern继续独占主提示词、角色卡、示例、AN、聊天历史、实际Token预算与最终组装。
- “校准ST插入”只修正管理条目的原生位置与顺序，不重置关键词、常驻、递归、向量或基石状态；玩家明确执行时也会校准旧基石。
- 人物支持唯一主焦点；焦点只调整原生召回并防止删除或被总结改换身份，`【条目】`直接关联可跳转。
- 手记支持单项折叠展开、文件夹折叠与排序、同名精确合并、安全删除、条目排序和文件夹内独立分页。
- 面板固定外壳，异步数据先离屏完成再原子显示；任务状态局部更新，不清空操作页面。
- 插件内部保存产生的宿主更新事件会延迟到队列提交边界统一同步，避免同一次操作连续重建页面。
- 手记展开项直接提供主焦点、关键词触发、常驻、递归、角色定义位置与基石设置；基石项在同一区域保留“解除基石”，玩家编辑仍可用。
- 管理选择复用类型图标槽位，手机端批量与文件夹操作采用紧凑工具栏，任务反馈不再替换按钮文字。
- 自动创建的聊天世界书会同步打开宿主编辑器；普通写入不抢占其他编辑器，并可从运行页定位本次更新。
- 世界设定文本与 TXT 共用 AI 预览链并统一开启基石保护；手动条目使用同一保护标记。
- 支持游戏时间锚点、世界书健康概览、严格 JSON 兼容、提取传输副本敏感词替换。
- 旧格式迁移严格限定为当前标题和正文模板，先扫描预览、确认写入、保留 UID，并可撤销。
- 维护页提供三轮真实验收、诊断 JSON 导出、玩家完整审核提示词与完整重置。
- 审核的语义标准完全由玩家提示词定义；插件只维护可解析的返回协议，不叠加隐藏审核规则。
- 总结来源标识与协议统一为裸 `条目N`，错误会同时显示无效编号和当前批次合法集合。

## 安装前提与唯一副本

支持范围从 SillyTavern `>= 1.18.0` 开始。这是 `manifest.json` 声明的客户端下限，不等同于尚未完成的目标环境实机验收结论。

每个 SillyTavern 实例只能保留一个镜渊副本。停止 SillyTavern 后，同时检查用户目录 `data/<handle>/extensions/` 与全局目录 `public/scripts/extensions/third-party/`，按大小写不敏感方式移走所有旧副本、版本号目录和大小写变体，只保留一个名为 `mirror-abyss/` 的目录；不要同时安装用户副本与全局副本。随后重启 SillyTavern 并强制刷新页面。

SillyTavern 1.18.0 的扩展“发现”顺序与浏览器静态资源“实际取文件”顺序并不等价：全局 `public/` 静态路由先于用户扩展回退路由注册。因此，同名全局旧副本存在时，扩展管理器即使把用户副本列为 `local`，浏览器仍可能取得全局旧版的 `manifest.json`、`index.js` 和 `app.js`。用户目录不能作为同名全局副本的覆盖升级层；必须先移走全局旧副本再启动。启动后应直接打开实际服务地址下的 `/scripts/extensions/third-party/mirror-abyss/manifest.json`，确认 `version` 为 `4.0.34`；手工部署包还应为 `auto_update=false`，并确认页面中只出现一个镜渊 root、launcher 与聊天状态入口。

## GitHub（源码仓库）安装

将源码包内 `Mirror-Abyss-4.0.34/` 的内容放在 GitHub 仓库根目录；`manifest.json`、`index.js`、`app.js` 与 `style.css` 必须位于根目录，不能只上传 ZIP。然后在 SillyTavern 的“扩展 → 安装扩展”中粘贴该仓库地址。

源码仓库的 `manifest.json` 保持 `auto_update=true`，供 SillyTavern 的 Git 安装／更新流程使用。`index.js` 是静态生命周期入口，加载构建产物 `app.js`；`style.css` 由宿主按清单加载，运行时不会请求 `src/`。

## 部署包手工安装

部署 ZIP 只包含宿主运行所需文件，归档内固定为唯一顶层目录 `mirror-abyss/`。普通用户优先解压到当前用户作用域，最终形成：

```text
data/<handle>/extensions/mirror-abyss/manifest.json
```

只有管理员需要为所有用户提供扩展时，才安装到全局作用域：

```text
public/scripts/extensions/third-party/mirror-abyss/manifest.json
```

部署包内的专用 `manifest.json` 固定为 `auto_update=false`，不会从 Git 仓库自动更新；升级时在同一选定作用域替换该目录，并再次执行“跨作用域、大小写不敏感、只留一个副本”的检查。不要把 ZIP 本身放入插件目录，也不要形成 `mirror-abyss/mirror-abyss/` 双层嵌套。

同一用户、聊天与世界书的验收和正常使用应保持单个 SillyTavern 页面。`RuntimeLease` 与共享锁只覆盖当前 JavaScript realm；不同浏览器页面不是同一个进程内协调域，仍可能并发编辑同一宿主资源。

部署归档中的目录权限统一为 `0755`、文件权限统一为 `0644`。源码包仍使用带版本号的顶层目录，用于 GitHub 发布和继续开发。

## 源码包验证（部署包不包含这些工具）

以下命令只能在完整源码仓库／源码 ZIP 中执行；部署 ZIP 没有 `src/`、`scripts/`、`tests/` 和 `package.json`，安装部署包的用户应跳过本节。源码工具前提为 Node.js `20` 或更高版本，并且 `zip`、`unzip` 均已加入 `PATH`。

```bash
npm run build
npm run verify
npm run package
```

打包器在临时 staging 中生成并验证候选，再逐文件原子替换；失败会回滚已替换文件、清理 staging，并保留旧正式产物。它只规范化 staging 内归档条目和新生成文件，不会 `chmod` 任意既有输出目录；归档会精确检查单一顶层、文件集合、权限、运行文件字节与 `auto_update`，源码包还会拒绝嵌套 ZIP 或 SHA／checksum 文件。

默认输出是仓库内受控的 `release/`。其他仓库内自定义 `--output` 会被拒绝，避免旧 ZIP、SHA 或输出目录被再次嵌入源码包；仓库外输出必须位于支持 POSIX 权限的文件系统，例如：

```bash
npm run package -- --output /path/on/posix-filesystem/mirror-abyss-release
```

4.0.34 的源码、运行文件或文档只要有任一字节变化，就必须重新执行本版本的构建、校验与打包并重算 SHA-256；不得复用 4.0.31 或其他候选的 ZIP／哈希。静态检查、单元测试和候选打包通过都不能写成 SillyTavern 实机 PASS，最终实机结论只能来自源码冻结后的真实环境验收记录。

不要直接编辑根目录 `app.js`；它由 `src/` 生成。架构边界和修改路由见 `ARCHITECTURE.md`，错误定性见 `ERROR-CATALOG.md`。维护页的“三轮完整验收”会调用当前模型路由并执行可回滚的临时世界书事务，因此会产生少量模型请求。
