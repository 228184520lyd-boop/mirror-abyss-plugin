# 验证报告

验证日期：2026-08-23

## 结果

- Node 语法检查：通过
- 自动化测试：20 / 20 通过
- 外部 npm 依赖：0
- 世界书读写者：1（`WorldbookRepository`）
- 长期事实存储：1（当前聊天绑定的世界书）

## 覆盖范围

- 单向依赖与无反向引用
- Controller 只编排用例，不持有世界书条目规则
- SillyTavern Context 与扩展生命周期契约
- 世界书唯一读写边界
- 错误 `source/code/cause` 定性
- 单一 Schema 被提示词、协议、合并和界面共用
- 只管理显式带镜渊标记的世界书条目
- “类型＋稳定名称”精确唯一性
- 提取复用 UID 与状态栏位替换
- 手动合并创建单一目标或复用已选 UID
- 纯文本与 TXT 共用同一导入预览、确认链
- 手机全视口、单主滚动面、16px 基准字号和44px操作高度

## 验证边界

本报告包含静态契约测试和隔离业务流程测试；未伪称在用户的 SillyTavern 实例中完成真机运行。部署后应使用“维护 → 运行诊断”校验当前聊天、模型和权威世界书。
