# Mirror Abyss 1.3.17

流水线：`ma-pipeline-80`

本版处理两个实机问题：状态模型输出过长，以及表格成功后世界书没有创建条目。

状态提示词改为在不遗漏明确事实的前提下使用最精炼、最简洁的表达；解析器不再用 220/320 字硬阈值拒绝事实。

世界书发布不再对成功状态表做第二轮相关性筛选。同步直接读取和写入服务端世界书文件，随后按托管键、备注和正文逐条回读验收。表格有可发布条目但计划为空，或服务端没有实际条目时，任务会失败而不是报告成功。

升级后保留现有表格数据，执行一次“立即同步”即可重新发布。
