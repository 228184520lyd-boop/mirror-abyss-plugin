# 错误来源目录

错误展示格式为 `【source/code】说明`。上层不得根据文案重新分类错误。

| source | 责任边界 |
|---|---|
| `host` | 当前聊天、消息快照、SillyTavern 生命周期 |
| `model` | 模型请求、瞬时重试、结构化结果重试 |
| `protocol` | 固定行协议、类型、栏目、临时来源编号 |
| `worldbook` | UID、唯一身份、写前一致性、提交与回滚 |
| `workflow` | 用例前置条件、任务互斥、玩家选择 |
| `ui` | 界面交互前置条件，不承担下层错误分类 |
| `core` | 不应跨层出现的基础不变量 |

排查顺序：先看 `source/code`，再进入对应模块；禁止在 UI 或 Controller 为下层错误增加语义兜底。
