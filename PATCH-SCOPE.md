# 2.0.0-lite.ui.49-scene-settlement patch scope

本轮只修改自动处理频率与场景结算链：

1. 自动审核仍逐轮运行。
2. 自动提取不再逐轮运行。
3. 只读取 AI 正文中明确的 `场景：...` 字段。
4. 没有场景字段时，该轮不参与场景判断、提取、总结或计数。
5. 标准化后的完整场景字段相同：继续当前场景，不调用提取模型。
6. 标准化后的完整场景字段不同：用旧场景正文执行一次提取和一次小总结，新正文作为下一场景起点。
7. 每成功结算一个场景，场景计数加一；累计六个场景后触发大总结并清零。
8. 不增加语义相似度、Token 阈值、地点层级判断、递归或第二剧情数据源。
9. 手动审核、提取、小总结和大总结继续可用。
