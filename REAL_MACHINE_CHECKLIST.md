# SillyTavern 实机检查单

目标版本：`2.0.0-alpha.4-realtest.1`  
状态值只能填：`未运行`、`通过`、`失败`。

## A. 构建与安装

| 项目 | 状态 | 证据/错误 |
|---|---|---|
| `npm install --no-audit --no-fund` | 失败 | 2026-07-26 实际执行；内部 registry 连续返回 HTTP 503 |
| GitHub 独立构建分支/内容写入 | 失败 | GitHub App 对 create branch 与临时 probe file 均返回 HTTP 403；主分支未被改动 |
| `npm run smoke` | 通过 | 34 个生产 TS/TSX 文件、相对导入、manifest、禁用概念、FACT/EVT 引用通过 |
| `npm run smoke:reducer` | 通过 | 旧事实替换、其他消息保留、人工锁定、时空单例通过 |
| `npm run build:installable` | 通过 | 生成本地 `dist/index.js`，以及 `esmsh`、`jsdelivr` 两套各 33 个 runtime 模块 |
| `npm run smoke:host` | 通过 | 模拟宿主验证事件注册/清理、稳定聊天键、消息键补写、正文回写、Swipe 同步与旧会话拒绝 |
| `npm run smoke:install` | 通过 | 67 个输出 JS 语法、双来源 loader、生命周期导出、版本、安装目录和导入映射通过 |
| `npm run typecheck` | 未运行 | npm 类型依赖未安装；转译构建不冒充正式 typecheck |
| `npm run build`（Webpack 自包含） | 未运行 | npm 依赖未安装 |
| 将安装包安装为 ST UI Extension | 未运行 |  |
| 扩展启用后面板出现且控制台无加载错误 | 未运行 |  |
| 目标浏览器可访问 `esm.sh` | 未运行 | 当前容器 DNS 无法解析该域名，不能代替目标浏览器结论 |
| `esm.sh` 失败后自动改用 `jsDelivr +esm` | 未运行 | 当前容器同样无法解析 jsDelivr；需在目标浏览器验证回退行为 |
| 点击“复制实机诊断 JSON” | 未运行 | 应可复制，且 JSON 不含正文、事实内容、审核规则和角色名称 |

## B. 宿主与聊天隔离

| 玩家步骤 | 预期 | 状态 | 证据 |
|---|---|---|---|
| 打开聊天 A | 面板显示 A 的 chatKey 与文档 | 未运行 |  |
| 切到聊天 B | A 任务作废，UI只显示 B | 未运行 |  |
| A→B→A 快速切换 | 第一次 A 的迟到结果不得写入第二次 A | 未运行 |  |
| disable → enable 两次 | 不出现重复事件和重复模型调用 | 未运行 |  |

## C. 核心消息闭环

| 玩家步骤 | 预期 | 状态 | 证据 |
|---|---|---|---|
| 关闭审核，点击“处理最新 AI 正文” | 仅调用一次提取，保存 ChatDocument，表计数变化 | 未运行 |  |
| 对同一正文再次点击 | 复用完成结果，不再次调用模型 | 未运行 |  |
| 启用审核并填写规则，正文通过 | 审核一次、提取一次 | 未运行 |  |
| 正文违规且自动修正开启 | 审核一次；必要时修正一次；正文回读一致后提取 | 未运行 |  |
| 修正后故意让提取失败 | 再次执行不得重新审核或重新生成修正文 | 未运行 |  |
| 模型返回空文本 | UI 明确显示“模型返回为空” | 未运行 |  |
| 模型返回错误固定文本 | UI 标明对应阶段，不覆盖旧 ChatDocument | 未运行 |  |

## D. 数据持久化

| 玩家步骤 | 预期 | 状态 | 证据 |
|---|---|---|---|
| 处理完成后刷新页面 | revision、消息阶段和表格仍存在 | 未运行 |  |
| Swipe 或编辑正文 | contentHash 改变，旧事实贡献在新提取成功后被替换 | 未运行 |  |
| 删除前方消息导致索引变化 | messageKey 仍定位同一消息；不能写到另一条 | 未运行 |  |

## 实机结论

- 当前结论：**已生成并完成本地候选回归，尚未在 SillyTavern 运行，不能宣称实机通过。**
- 运行环境：待填写。
- SillyTavern 版本：待填写。
- 浏览器/设备：待填写。
- 诊断 JSON 或控制台错误：待填写。
