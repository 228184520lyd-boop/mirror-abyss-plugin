# 固定事实驱动的修改协议

后续每一轮修改必须遵循以下循环：

1. **先找触发事实**：从 `FIXED_FACTS.md` 和禁止回归清单选出直接相关 ID。
2. **记录工程事件**：在 `ENGINEERING_EVENTS.md` 追加事件，写明真实现象，不能先写抽象猜测。
3. **只做最小修改**：修改必须直接消除事件，不顺便更换框架、概念或业务规则。
4. **更新固定事实**：新的实机证据形成新事实；旧事实失效时注明被哪个新事实取代，禁止删除历史。
5. **做简单验证**：依次运行 `npm run smoke`、`npm run typecheck`、`npm run build`。
6. **单独记录实机**：真实 SillyTavern 的结果只能写入实机清单，静态检查不得标成“实机通过”。
7. **循环**：下一次修改必须再次从固定事实和上一工程事件开始。

代码注释只在非显然边界使用：

```ts
// FACT-RUN-001：任务目标只在开始时固定，阶段中不得重新决定目标聊天。
// EVT-20260726-004 / FACT-CONTENT-002：正文替换后回读，防止“修正成功但玩家仍看到旧正文”。
```

禁止使用大量逐行注释，也禁止用注释掩盖无法解释的流程。
