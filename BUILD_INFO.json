{
  "name": "Mirror Abyss / 镜渊",
  "version": "1.1.0-rc.2",
  "baseline": "1.1.0-rc.1 local stability candidate",
  "baseline_commit": "417e4231a8c191a5f84e2ad0f81c682f170cfaed",
  "local_branch": "product/memory-contract-v1-rc.2",
  "build_date": "2026-07-15",
  "build_type": "local product-alignment candidate with simulated-player regression",
  "pipeline_contract": [
    "assistant_final_text",
    "admission_gate_optional_audit",
    "active_tables",
    "event_operations_and_stage_overview",
    "derived_event_registry",
    "large_summary_and_event_sedimentation",
    "entry_level_lorebook_publish"
  ],
  "event_activation_modes": [
    "constant",
    "keyword",
    "vector"
  ],
  "event_precision_order": [
    "exact",
    "compressed",
    "fuzzy",
    "trace"
  ],
  "graph_mode": "read_only",
  "validation": "ES Module parse/import, strict event-evidence protocol retry, bounded constant scene anchor, honest recall diagnostics, simulated 95-fact player session, live batch drain, refresh queue isolation, 504 adaptive split, checkpoint resume, local-commit recovery, history rebuild stage resume, cancellation/stale release, deployment syntax/version and ZIP integrity checks",
  "limitations": "Original TypeScript project, package manifest and full source test suite were not present in the uploaded deploy archive.",
  "connection_error_handling": [
    "detect_html_before_fact_protocol_parsing",
    "identify_current_or_named_connection_profile",
    "classify_http_gateway_path_auth_failures",
    "suppress_duplicate_low_level_json_parser_cause",
    "avoid_retrying_non_transient_html_configuration_failures"
  ],
  "fixes": [
    "disable manifest auto-update while the configured GitHub main branch remains behind this local deployment baseline",
    "reject and protocol-retry newly generated event entries or long-term candidates that do not cite a fact or changed row from the current bounded batch",
    "preserve older unverified event entries for compatibility while exposing their missing provenance in diagnostics instead of deleting them",
    "publish one constant scene anchor capped at 1600 characters with only the current focus and two newest spacetime rows",
    "state explicitly that successful worldbook publish and server readback do not prove a particular main-text generation actually received the entry",
    "show planned versus effective event activation including vector-to-keyword fallback without claiming unavailable host hit telemetry",
    "drain every small-summary batch planned by an ordinary live play trigger before attempting the large summary",
    "freeze a resumed small-summary queue to its persisted source IDs so facts arriving after refresh are deferred to a later trigger",
    "cap each small-summary request at 12000 budget characters and 24 changed facts using the complete system and user prompt plus protocol-retry reserve",
    "preserve fact-package and source order while splitting only between complete structured fact units",
    "replace an initial HTTP 504, gateway timeout or no-content upstream boundary failure with adaptive child batches instead of an identical retry",
    "commit each successful child batch immediately and persist remaining source IDs, batch descriptors, split lineage and generated summary IDs",
    "derive idempotent small-summary batch keys from chat, history revision, rebuild generation and ordered source IDs",
    "resume the first unfinished small-summary batch without replaying committed facts, tables, summaries, event entries or lorebook work",
    "show actual input size, budget use, fact count, batch progress, attempt, source range, split state and HTTP status in mobile task diagnostics",
    "represent missing first data as null and N/A instead of zero milliseconds",
    "abort the active history model request on user cancellation while retaining successful batches and the remaining checkpoint queue",
    "preserve exact failed small-summary source keys and input-boundary diagnostics when the local commit stage fails",
    "resume prepared, committing or committed small-summary transactions before dispatching another model request",
    "rebase only history-rebuild recovery metadata when the rest of a pending small-summary transaction still matches exactly",
    "never replay conflicted or cancelled summary transactions over newer chat state",
    "derive small-summary changed rows, affected operations and minimal context anchors in code instead of sending the complete current table snapshot",
    "bind historical small summaries to their fact-package stage snapshots so early summaries cannot observe later state",
    "consume no-event-change fact batches locally without a model call or long-term candidate",
    "preserve rebuilt facts and tables when a derived summary fails and retry only the exact failed source keys",
    "classify and expose small-summary input, truncation, empty, protocol and local-commit failures with non-sensitive boundary counts",
    "resume failed history fact batches from the earliest persisted checkpoint without removing completed batches",
    "resume small summary, large summary and final lorebook synchronization as separate stages",
    "preserve native model error kind, status, connection label and latest invocation timing through pipeline wrappers",
    "keep connection failures non-destructive and expose actionable history rebuild errors in task diagnostics",
    "restrict non-stream fallback to explicit Connection Profile stream contract and unsupported-interface failures",
    "never retry ordinary upstream, HTTP, network, parse, cancellation or stale failures as non-stream requests",
    "exclude the stale unreproducible source map from deployment archives",
    "record model-call queue, transport, first-data, response, parse and persistence timing without prompt or response content",
    "replace per-profile FIFO with a bounded priority scheduler",
    "limit each profile to two requests and derived summaries to one slot while serializing calls from the same chat",
    "move derived summaries out of the critical fact-extraction task tail",
    "aggregate native Connection Profile streams before parsing with one compatibility fallback",
    "disable automatic history rebuild by default",
    "perform local consistency detection only when entering or switching chats",
    "record detected history changes non-destructively without calling the model API",
    "pause table, summary and lorebook derivation until the player starts manual rebuild",
    "add an opt-in automatic history rebuild setting",
    "change edit, delete and swipe handling to detect-and-prompt when automatic rebuild is disabled",
    "keep existing memory intact until manual rebuild connection preflight succeeds",
    "show a dedicated history-change pending status instead of a fake running progress bar",
    "preserve adaptive extraction, model routing, admission, event memory and lorebook consistency behavior"
  ]
}
