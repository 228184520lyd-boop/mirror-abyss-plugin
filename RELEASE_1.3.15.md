# Mirror Abyss 1.3.15

流水线：`ma-pipeline-78`

本版收紧状态提取的事实边界：每条事实归属于真正发生变化的对象，不同对象的不同变化可分别保存，同一对象的同一变化只能出现一次。

`<MA_CORE>` 不再强制。事件没有独立过程事实时，只使用事件容器组织对象变化；插件本地维持事件名称、状态与关联对象，避免事件整体动作与对象结果重叠。

状态提示词改为稀疏正向引导：正文未点名或未明确证实不生成，不遍历旧对象，不补齐类型，不按常识推导。
