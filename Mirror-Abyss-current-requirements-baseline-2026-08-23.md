# Mirror Abyss 当前需求与源码基线（2026-08-23）

## 1. 文档用途

本文件用于承接上一对话中未能完整转移的需求，并约束后续源码审计、修改与候选包交付。

记录分为四类：

- **冻结需求**：后续直接执行，不再以相似说法重新讨论。
- **代码事实**：已由当前附件源码或可重复基准确认。
- **历史说明**：只说明版本演进，不自动成为当前修改目标。
- **待验证项**：没有足够源码或实机证据，禁止宣称已经完成。

## 2. 当前附件身份

### 2.1 当前工作基线

`Mirror-Abyss-interface-only-source.zip`

- 内部版本：`3.0.0-lite.ui.9.1-runtime-repair`。
- 包含部署文件 `manifest.json`、`index.js`、`app.js`、`README.md`、`LICENSE`。
- 额外包含四个代码级基准：`dead-code-audit.mjs`、`debt-cleanup-benchmark.mjs`、`extraction-chain-benchmark.mjs`、`engineering-chain-benchmark.mjs`。
- `app.js` 是包含 26 个内部模块的打包文件；附件没有独立的 `src/` 源码目录。

最终工作树已在该基线上收束为 `3.0.0-lite.ui.10-final-candidate-v1`，并新增 `runtime-chain-benchmark.mjs`；上述 ui.9.1 信息保留为输入附件身份记录。

### 2.2 当前部署基线

`Mirror-Abyss-interface-only-deploy.zip`

- 内部版本同为 `3.0.0-lite.ui.9.1-runtime-repair`。
- 五个部署文件与 source 包中的对应文件逐字节一致。
- 后续生成候选部署包时，部署内容必须从最终 source 工作基线的这五个文件产生。

### 2.3 历史对照基线

`Mirror-Abyss-3.0.0-lite.ui.6-extraction-chain-clean.zip`

- 用于核对 ui.6 已完成的提取链清理。
- 不是当前候选包，也不覆盖 ui.7、ui.8、ui.9、ui.9.1 的后续修改。

## 3. 冻结架构需求

### 3.1 唯一事实源

- 世界书是唯一长期剧情事实源。
- 禁止建立第二剧情数据库、隐藏事实缓存、独立记忆层或插件侧剧情状态机。
- 玩家直接编辑世界书后，下一次提交必须以刚读取的权威世界书为基线。

### 3.2 模型职责

模型负责：

- 理解玩家输入与 AI 正文。
- 判断哪些明确发生的变化足以成为后续连续性事实。
- 提取语义、选择合法栏目并生成总结结果。

只提取：

- 已明确发生的事实。
- 会影响后续世界运行的重要变化。
- 连续性锚点。

不提取普通过程、未确认信息、推测和未来目标。

### 3.3 插件职责

插件负责：

- 批次与任务生命周期。
- UID 管理。
- 协议外形与字段合法性校验。
- 确定性匹配、操作计划和数据事务。
- 世界书保存、权威回读、校验、失败回滚。
- UI 展示。

插件不得判断剧情发展、人物意图或事实重要性，不得推断事实、自动补写剧情或替模型完成语义判断。

### 3.4 修改边界

- 当前工作不是重构架构，也不新增系统。
- 优先修复违反冻结原则、调用链错误、真实死代码、职责越界和文档/基准失真。
- 不以减少行数为目标。
- 不增加新的 Prompt 来源、模型业务链、Provider、模型管理系统、世界书结构、UID 体系或写入入口。
- 删除旧链时不做隐藏迁移，不保留可重新生效的旧设置或旧行为来源。
- 必要的同 Prompt 协议重试保留。

## 4. 当前提取链固定事实

### 4.1 唯一生产链

正式提取链为：

```text
runTask('extraction')
→ MemoryRunner.extract()
→ extractionPrompts()
→ 模型请求
→ parseExtractionProtocol()
→ 确定性操作计划
→ 世界书事务提交
```

已删除诊断提取旁路、独立验收写入路径、隐藏 `settings.extractionPrompt`、提取专用 compact Prompt、失败原因回灌和重复协议注入。

### 4.2 唯一固定事实协议

模型的标准输出协议为：

```text
事实｜类型｜稳定名称｜栏目｜建立/变化/结束｜关联对象｜完整事实
```

合法类型为：人物、场景、物品、事件、世界。

UID 不提供给模型。模型只处理语义对象、栏目和事实；UID 命中与写回由插件执行。

### 4.3 重试边界

- 首次模型请求成功且输出形成协议：只调用一次。
- 首次请求成功但最终文本未形成协议：原样重放同一 Prompt、同一响应预算一次。
- 连续两次协议失败：世界书不写入，处理游标不推进。
- 请求失败、鉴权或参数错误、任务取消、解析器自身异常：沿原错误边界结束，不作为协议失败再次调用模型。

### 4.4 当前格式兼容层

当前 `app.js` 实际存在两类机械兼容：

- 去除单行固定事实外层的列表标记、成对括号或成对引号。
- 对可完整映射到七字段的 JSON 对象或对象数组进行机械字段转换。

最终候选进一步接受 Markdown JSON 外壳和说明文字中的第一个完整 JSON 对象/数组。兼容层不建立第二协议；它只把明确键映射回同一七字段协议。类型、稳定名称、栏目、变化、关联对象、完整事实任一键缺失即拒绝，且不再为缺失的变化字段默认补“变化”。

兼容层只允许转换外形，不得补充缺失语义、推断类型、推断栏目或改写事实。

此处是当前代码事实，也是此前提出的“极薄 JSON 格式兼容层”需求。README 的 ui.9.1 说明目前只写了括号/引号去壳，没有记录 JSON 转换，后续需要统一文档、基准与实现表述。

### 4.5 提取前薄清洗

- 只在发送给提取模型的传输副本上进行固定词面替换，覆盖小范围成人行为、显式生殖部位和体液表达。
- 替换采用一对一的中性/临床表达，不做上下文推断，不扫描普通身体描述，不补字段。
- 原聊天正文、世界书和事务快照均不修改；该层不是剧情重写链，也不是第二事实源。

## 5. 当前运行与工程链固定事实

### 5.1 自动正文触发

ui.9.1 当前监听：

- `MESSAGE_RECEIVED`：作为部分宿主环境的兼容入口。
- `GENERATION_ENDED`：完整生成结束时立即调度。

非立即调度使用 650ms 延迟并再次等待 220ms 比较正文哈希；正文变化时重新调度。`pendingMessageTimers` 在停用、聊天切换、重置、验收模式切换和任务取消边界清理。

这组逻辑用于修复仅监听 `GENERATION_ENDED` 时自动链不触发或触发过早的问题，不属于第二提取链。

### 5.2 当前单一权威

- 场景阶段：`governance.resolveSceneStages`。
- 基础设定识别：`semantic.isFoundationEntry`。
- SceneGroup 身份：`groupUid`。
- 时间线规范化：`util.normalizeEventTimeline`。
- 玩家焦点持久化：世界书条目扩展的 `focus` 字段。
- 回合回滚：`turnRollbackSnapshots`；`commitReceipts` 不再兼作旧快照迁移来源。

### 5.3 保存与回滚边界

正式写入顺序为：

```text
读取权威世界书
→ 确定性应用操作
→ 使用宿主正式接口保存
→ 权威回读
→ 校验
→ 失败时恢复本次 UID 前态
```

- 不保存整本世界书历史快照。
- 每个真实剧情回合只保存该回合实际改动 UID 的第一次修改前完整条目。
- 同一回合多次修改同一 UID 仍只保留最早前态。
- Edit、Swipe、Delete 取消镜渊自身的运行与排队任务，并在独占边界内恢复受影响 UID；不阻塞 SillyTavern 自身操作。
- 读写窗口内世界书或聊天来源变化时，旧任务不得继续提交。

## 6. 当前总结与整理链事实

- 小总结：同批细颗粒条目整理为中颗粒条目。
- 大总结：多个已经结算的独立 SceneGroup 保持边界，整理为长期粗颗粒条目。
- 模型看到临时 `条目1 / 条目2 / ...`，看不到 UID。
- 返回原临时编号时保持原 UID 并整条更新。
- 返回 `新条目N` 时创建新 UID。
- 本批未返回的原条目按当前完整条目整理协议删除。
- 小总结、大总结、选中总结与人工合并共用完整条目整理方式。
- 整本世界书整理为一次完整输入、预览、确认提交；失败保留原书，成功后允许撤销一次。

这些属于当前 ui.9.1 代码与 README 所描述的现行链。更早交接文档中的物品、临时 NPC、人物性格受限沉降方案属于旧阶段记录，不能未经当前源码验证直接重新加入。

## 7. 已完成的代码级验证

### 7.1 附件一致性

- source 包与 deploy 包的五个部署文件哈希完全一致。
- 两包内部版本、加载缓存键和 README 版本一致，均为 `3.0.0-lite.ui.9.1-runtime-repair`。

### 7.2 已通过

- `extraction-chain-benchmark.mjs`：通过。
  - 首轮合法协议一次成功。
  - 明确“无”一次成功。
  - 首轮协议失败后只原样重放一次。
  - 连续协议失败确定报错。
  - 401 与任务取消均不重放。
- `engineering-chain-benchmark.mjs`：通过。
  - 26 个模块可加载且生产入口可达。
  - 提取输入、确定性计划和投影输出完整。
  - 场景、焦点、时间线身份保持单一权威。
  - 审核、修正、世界设定和设置边界检查通过。

### 7.3 已修复的失真基准

- `dead-code-audit.mjs` 报告 `eventText` 无引用。
  - 实际代码在模板字符串表达式 `${eventText(entry)}` 中调用。
  - 审计脚本会整体剥离模板字符串，因此漏掉该调用。
  - 当前证据表明这是审计器误报，不是可直接删除的死代码。
- `debt-cleanup-benchmark.mjs` 的 `oneAutomaticGenerationEvent` 与 `noGenerationPollingBranch` 失败。
  - 脚本仍冻结在 ui.8 的“只监听 GENERATION_ENDED、无稳定检测”目标。
  - ui.9.1 已明确恢复双事件入口和 650/220ms 稳定检测。
  - 这是旧基准与当前运行修复冲突，不能据此删除 ui.9.1 运行链。

最终候选已修正两处基准：死代码审计会识别模板字符串表达式引用；技术债基准改为验证双事件兼容、稳定检测、无运行时事实缓存和无插件临时身份推断。新增运行时行为基准确认同一正文的两个正常宿主事件只进入一次正式任务队列。

## 8. 已解决的不一致与剩余实机边界

### 8.1 文档与实现不一致

- README 的 ui.9.1 运行修复只记录外层括号/引号去壳；代码还包含 JSON 对象机械转换。
- README 表述“完成工程质量验收”，但随 source 包交付的两个审计脚本当前退出码为 1。
- `dead-code-audit.mjs` 无法正确识别模板字符串表达式中的函数引用。
- `debt-cleanup-benchmark.mjs` 没有随 ui.9.1 更新验收目标。

上述四项均已在最终候选解决：README、解析测试、自动事件测试和审计目标已经与实现统一。

### 8.2 测试覆盖缺口

- 提取链基准没有覆盖外层括号、引号、列表标记和 JSON 机械转换。
- 自动触发基准没有验证同一正文经两个宿主事件只进入一次正式任务队列。
- 没有当前附件对应的真实 SillyTavern 长时间游玩验收记录。

前三项测试覆盖缺口已经补齐；真实 SillyTavern 长时间游玩仍是唯一未由容器内代码测试替代的外部验收边界。

### 8.3 源码形态限制

- 当前 source 包只有打包后的 `app.js` 和基准脚本，没有独立 `src/` 模块树、构建配置或构建命令。
- 可以审计和修改打包文件，但无法从当前附件证明它与某个未提供的模块化源码仓库逐文件一致。

## 9. 执行结果

1. 已以本文件和 `interface-only-source` 为唯一工作基线。
2. 已修正失真的审计/基准，没有删除 ui.9.1 的双事件运行修复。
3. 已补齐机械格式兼容、缺字段拒绝、薄清洗和双事件去重测试。
4. 已沿生产调用链确认单一来源、职责边界、事务回读/回滚及取消边界；并发现、移除运行时事实缓存和插件端临时身份推断。
5. 已统一内部版本、manifest、加载缓存键和 README。
6. 最终交付包固定命名为 `Mirror-Abyss-final-candidate-v1.zip`；另附 source 候选包、最终修改清单和本基线文档。

## 10. 候选包完成标准

候选包只有同时满足以下条件才能标记为代码级候选：

- 当前冻结架构和职责边界未被破坏。
- 提取仍只有一条生产链和一份 Prompt 来源。
- 格式兼容层保持机械转换，不产生语义判断。
- 保存、权威回读、失败回滚和来源取消闭环成立。
- source 与 deploy 的部署文件逐字节一致。
- 所有随包基准均与当前目标一致并通过。
- README、版本号、缓存键、实现和变更清单一致。
- 明确写出尚未完成的 SillyTavern 实机长玩验证，不以静态测试代替实机结论。

最终部署候选的精确文件名冻结为：`Mirror-Abyss-final-candidate-v1.zip`。
